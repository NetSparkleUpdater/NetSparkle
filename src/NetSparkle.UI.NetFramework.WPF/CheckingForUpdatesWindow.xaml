<Window x:Class="NetSparkle.UI.NetFramework.WPF.CheckingForUpdatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:NetSparkle.UI.NetFramework.WPF"
        mc:Ignorable="d"
        Width="400"
        Height="145"
        Title="Software Update"
        ResizeMode="NoResize"
        MaxWidth="400"
        MaxHeight="145"
        Name="CheckingForUpdateWindow"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Grid.RowSpan="2"
               Source="{Binding ElementName=CheckingForUpdateWindow, Path=Icon}"
               VerticalAlignment="Top"
               HorizontalAlignment="Left" Width="48" Height="48" Margin="10,10,0,0"/>
        <Label Grid.Row="0" Grid.Column="0" Content="Checking for Updates..." HorizontalAlignment="Center" FontWeight="Bold" FontSize="24"/>
        <ProgressBar Grid.Row="1" Grid.Column="0" IsIndeterminate="True" Height="25" Width="200" Margin="0,5,0,0"/>
        <Button Grid.Row="2" Grid.Column="0" Content="Cancel" FontSize="16" Width="100" Margin="0,10,0,0" Click="CancelButton_Click"/>
    </Grid>
</Window>
